import { FilterActorType } from "../api/types/actorTypes"


interface FiltersProps {
    setFilters: (filter: FilterActorType) => void
}

export default function Filters(props: FiltersProps) {
    

    return (
        <div>
            
        </div>
    )
}